import { Meta, ArgsTable, Story, Canvas } from '@storybook/addon-docs/blocks';
import { action } from '@storybook/addon-actions'
import { linkTo } from '@storybook/addon-links'
import DetailsNaughtDialog from '@/core/components/naughtDialog/DetailsNaughtDialog.vue';
import { getCurrentBreakpoint } from '@/core/utils/breakPointUtil';

<Meta title="Components / Dialog " component={DetailsNaughtDialog} />

<ArgsTable of={NaughtDialog} />

Shows an example DetailsNaughtDialog (Overwridden BaseNaughtDialog).

<Canvas>
  <Story name="Details Naught Dialog">
    {{
        components: { DetailsNaughtDialog },
        computed: {
    		  currentBreakpoint() {
			      return getCurrentBreakpoint(this);
		      }
	      },
        template: `
          <div :class="currentBreakpoint">
            <details-naught-dialog />
          </div>
          `
    }}
  </Story>
</Canvas>