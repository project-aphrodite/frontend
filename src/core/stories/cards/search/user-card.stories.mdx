import { Meta, ArgsTable, Story, Canvas } from '@storybook/addon-docs/blocks';
import { action } from '@storybook/addon-actions'
import { linkTo } from '@storybook/addon-links'
import UserCard from '@/core/components/cards/search/UserCard.vue';
import { getCurrentBreakpoint } from '@/core/utils/breakPointUtil';

<Meta title="Components " component={UserCard} />

<ArgsTable of={UserCard} />

Shows an example UserCard.

<Canvas>
  <Story name="UserCard">
    {{
        components: { UserCard },
        computed: {
    		  currentBreakpoint() {
			      return getCurrentBreakpoint(this);
		      }
	      },
        template: `
          <div :class="currentBreakpoint">
            <user-card />
          </div>
          `
    }}
  </Story>
</Canvas>
