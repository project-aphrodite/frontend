import { Meta, ArgsTable, Story, Canvas } from '@storybook/addon-docs/blocks';
import { action } from '@storybook/addon-actions'
import { linkTo } from '@storybook/addon-links'
import SearchResult from '@/pages/search/components/SearchResult.vue';
import { getCurrentBreakpoint } from '@/core/utils/breakPointUtil';

<Meta title="Search Results/ Components " component={SearchResult} />

<ArgsTable of={SearchResult} />

Shows an example SearchResult.

<Canvas>
  <Story name="SearchResult">
    {{
        components: { SearchResult },
        computed: {
    		  currentBreakpoint() {
			      return getCurrentBreakpoint(this);
		      }
	      },
        template: `
          <div :class="currentBreakpoint">
            <search-result />
          </div>
          `
    }}
  </Story>
</Canvas>
