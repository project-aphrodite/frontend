import { Meta, ArgsTable, Story, Canvas } from '@storybook/addon-docs/blocks';
import { action } from '@storybook/addon-actions'
import { linkTo } from '@storybook/addon-links'
import UserCollections from '@/core/components/UserCollections.vue';

<Meta title="Components / User Collections" component={UserCollections} />

# Button

<ArgsTable of={UserCollections} />

Shows an example UserCollections, contains a collection and a catalogue (which is a collection with different data).

<Canvas>
  <Story name="User Collections">
    {{
        components: { UserCollections },
        template: `
          <user-collections/>
          `
    }}
  </Story>
</Canvas>