import { Meta, ArgsTable, Story, Canvas } from '@storybook/addon-docs/blocks';
import { action } from '@storybook/addon-actions'
import { linkTo } from '@storybook/addon-links'
import CreationDialog from '@/core/components/naughtDialog/CreationDialog.vue';
import { getCurrentBreakpoint } from '@/core/utils/breakPointUtil';

<Meta title="Components / Dialog " component={CreationDialog} />

<ArgsTable of={CreationDialog} />

Shows an example CreationDialog (Overwridden BaseNaughtDialog).

<Canvas>
  <Story name="Creation Dialog">
    {{
        components: { CreationDialog },
        computed: {
    		  currentBreakpoint() {
			      return getCurrentBreakpoint(this);
		      }
	      },
        template: `
          <div :class="currentBreakpoint">
            <creation-dialog />
          </div>
          `
    }}
  </Story>
</Canvas>